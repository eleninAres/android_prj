<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_LOOPBACK_INTERFACE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Os17Demo">
        <activity android:name=".os16cases.Os16CaseMActivity" />
        <activity android:name=".os16cases.Os16CaseLActivity" />
        <activity android:name=".os16cases.Os16CaseKActivity" />
        <activity android:name=".os16cases.Os16CaseJActivity" />
        <activity android:name=".os16cases.Os16CaseIActivity" />
        <activity android:name=".os16cases.Os16CaseHActivity" />
        <activity android:name=".os16cases.Os16CaseGActivity" />
        <activity android:name=".os16cases.Os16CaseFActivity" />
        <activity android:name=".os16cases.Os16CaseEActivity" />
        <activity android:name=".os16cases.Os16CaseDActivity" />
        <activity android:name=".os16cases.Os16CaseCActivity" />
        <activity android:name=".os16cases.Os16CaseBActivity" />
        <activity android:name=".os16cases.Os16CaseAActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="stateAlwaysVisible|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
